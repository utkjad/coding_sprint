def question(number):
	num_lst = [int(i) for i in list(str(number))]
	last_index = -1
	last_num = 42
	list_of_new_numbers 

	for i in range(1, len(num_lst)):
		if ( (num_lst[i-1] == num_lst[i]) and last_index < i and last_num != num_lst[i]):














if(__name__ == "__main__"):
	number = long(raw_input())
	question(number)
